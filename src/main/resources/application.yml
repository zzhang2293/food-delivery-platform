
spring:
  application:
    name: delivery-platform
  datasource:
     driver-class-name: com.mysql.cj.jdbc.Driver
     password: 01*gbcfd&wzn
     username: root
     url: jdbc:mysql://localhost:3306/delivery
     type: com.alibaba.druid.pool.DruidDataSource
#  main:
#    banner-mode: off

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # open mp log
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_id
    banner: false
server:
  port: 80

